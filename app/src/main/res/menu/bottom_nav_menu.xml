<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1) Graphs (btnGraph) -->
    <item
        android:id="@+id/nav_graph"
        android:icon="@drawable/ic_insert_chart"
        android:title="Graphs" />

    <!-- 2) Surrounding (btnSurrounding) -->
    <item
        android:id="@+id/nav_surrounding"
        android:icon="@drawable/ic_thermostat"
        android:title="Surroundings" />

    <!-- 3) Home (btnHome) -->
    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/ic_opacity"
        android:title="Home" />

    <!-- 4) Photo (btnPhoto) -->
    <item
        android:id="@+id/nav_photo"
        android:icon="@drawable/ic_local_florist"
        android:title="Photo" />

    <!-- 5) Settings (btnSettings) -->
    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/ic_settings"
        android:title="Settings" />

</menu>
